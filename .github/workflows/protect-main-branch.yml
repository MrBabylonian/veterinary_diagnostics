name: Protect Main Branch

on:
  push:
    branches:
      - main

jobs:
  prevent-push:
    runs-on: ubuntu-latest
    steps:
      - name: Prevent direct push to main
        run: |
          echo "::error::Direct pushes to the main branch are not allowed. Please create a pull request instead."
          exit 1
